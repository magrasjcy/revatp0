namespace  com.pizzaworld.Orders {
    class Pizza {
       private double unitcost; 
       private uint count;
       private uint size;
       private	uint[] toppings;
       public Pizza(uint quantity, uint size, uint count) {
	   this.count=quantity;
	   this.size=size;
	   this.count=count;
       }
    }
}
